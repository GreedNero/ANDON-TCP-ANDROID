package com.cia.tcpapp

// Rutas usadas por Navigation Compose.
object AppRoutes {
    const val TcpMainScreen = "tcpScreen"
    const val SecondScreen = "SecondScreen/{screenType}"
    const val LoginScreen = "LoginScreen/{screenType}"
    const val MaintenanceScreen = "maintenanceScreen"
}
